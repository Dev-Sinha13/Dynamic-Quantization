# T4 thinking-mode run at 320 tokens: 2026-09-03

## Outcome

The updated quality gates worked as intended. The first sample completed with a
correct answer, but the second sample reached the 320-token generation limit
before EOS. Execution stopped before receiver-head discovery and ZIP export, so
this run produced no accepted experiment bundle.

## Provenance

- Executed notebook: `AnchorKV_T4_Standalone (1).ipynb`
- Notebook SHA-256:
  `C312DCB0A64BC9A8622887B1D4223CF801DED324C93CBFC96DE267FB868D821C`
- GPU: Tesla T4, 14.6 GiB
- Model: `Qwen/Qwen3-0.6B`
- Model revision: `c1899de289a04d12100db370d81485cdf75e47ca`
- Thinking mode: enabled
- Maximum new tokens: 320
- Maximum total sequence: 768

## Observations

`math-000` completed normally:

- Prompt tokens: 49
- Generated tokens: 305
- Total sequence tokens: 354
- Peak allocated GPU memory: 1.335 GiB
- Validated answer: `$28`

`math-001` reached 320 generated tokens without EOS and was rejected. The third
sample did not run, and subsequent summary, head-discovery, plotting, and ZIP
cells were not executed.

The successful 354-token replay used far less than the T4's available memory.
The notebook's conservative 768-token lower-bound estimate remains only 1.69
GiB, so increasing the generation allowance does not exceed the existing
sequence or memory boundary.

## Corrections made before the next run

1. Increase `max_new_tokens` from 320 to 512 while retaining the 768-token
   total-sequence ceiling and EOS requirement.
2. Keep answer validation and the six-reasoning-span minimum enabled.
3. Exclude tag-only `<think>` and `</think>` spans from anchor candidates.

These changes landed in commits `d7d9e75` and `59fdae0`.
